# Lahiru's Portfolio

This is a minimal starter portfolio scaffold.

Quick start:

```powershell
# open the folder and serve with a static server, for example with VS Code Live Server
start .
```

Files:
- `index.html` — main page
- `styles/style.css` — styles
- `src/main.js` — JS entry
